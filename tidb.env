export CURRENT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
export ROOT_DIR="$(dirname "$CURRENT_DIR")"

export KEY_PAIR_NAME="tidb-key"
export KEY_SAVE_PATH="$ROOT_DIR/.ssh/${KEY_PAIR_NAME}.pem"